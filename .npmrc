# engine-strict can make `npm install` fail in environments with a different Node than
# local dev (including some Dependabot updaters). CI pins Node in `.github/workflows/pages.yml`.
engine-strict=false
